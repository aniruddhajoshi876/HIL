# CarMaker 12.0.1 ↔ Speedgoat Baseline interface research

## Executive summary

- The documented close-coupled route is to put CarMaker in the **same generated/compiled real-time application** as the control model. CarMaker for Simulink is an S-function integration, and its blocks run at CarMaker's fixed application step (default 1 ms); the Simulink-Coder route instead makes a CarMaker executable containing generated model code. Neither local manual identifies Speedgoat as a supported CarMaker/HIL target, so applicability to TargetPC1 is **not verified**. [ProgrammersGuide.pdf, §6, p. 172; §6.2.1, p. 180; §8.2, pp. 269-270]
- If that target integration can be licensed/supported, expose only the closed-loop boundary needed by the bench: driver/pedal demand into CarMaker and vehicle longitudinal/wheel-speed/torque-relevant feedback out, as explicitly named Simulink signals or CarMaker dictionary quantities. The CarMaker subsystem documents Driver/VehicleControl and powertrain/wheel interfaces; the exact chosen signals and units remain an engineering design decision. [ProgrammersGuide.pdf, §6.3, pp. 228-234]
- A physical CAN route is documented on the CarMaker/HIL side: its Rest Bus Configurator imports a CANdb (`.dbc`), separates real from simulated ECUs, maps signals to CarMaker Data Dictionary quantities/functions, and selects a configured physical CAN channel. This makes `MFE26_Inverter.dbc` a plausible **CarMaker-side contract**, but it does not make CarMaker's CAN interface appear on the Speedgoat automatically. A physical CAN adapter/channel on the CarMaker real-time host and a terminated bus still have to exist. [UsersGuide_HIL.pdf, §4, pp. 53-56, 58, 68]
- Do not treat IPGControl, the CM4SL GUI TCP port, or XCP-on-Ethernet as a documented deterministic Speedgoat plant-data link. The local manuals document IPGControl visibility of Data Dictionary quantities, GUI connection on TCP 12350, and XCP configuration for an ECU/A2L with CAN or Ethernet transports; they do not document a generic CarMaker-to-Speedgoat real-time signal protocol. [ProgrammersGuide.pdf, §6.1.1, p. 174; §6.2.3, pp. 182-185; UsersGuide_HIL.pdf, §5.1-5.2, pp. 81-85, 101-107]

## 1. Real-time coupling mechanisms actually evidenced in the local documentation

### A. CarMaker embedded in a Simulink/real-time application — documented concept; Speedgoat support unverified

CarMaker for Simulink is described as an S-function/API integration inside Matlab/Simulink, explicitly “not a loosely coupled co-simulation.” The CarMaker blocks are connected like Simulink blocks. The blockset runs direct-feedthrough at the fixed CarMaker application step (default 1 ms), independently of the rest of the model; its block chain must execute in order and exactly once, with no algebraic loops through individual CarMaker blocks. [ProgrammersGuide.pdf, §6, p. 172; §6.2.1-6.2.2, pp. 180-181]

For a generated application, the Simulink Coder interface generates C code and links it with CarMaker libraries so that the Simulink model is an integral part of the CarMaker executable, “standalone or under realtime conditions.” The manual’s listed HIL architectures are Xpack4, dSPACE DS1006/SCALEXIO, and NI PXI (through the corresponding templates); the HIL user guide gives detailed build flows for dSPACE and NI, not Speedgoat. Therefore, **the integration pattern is documented, but a supported CarMaker 12.0.1 build/deployment to this Speedgoat Baseline target is not established by the installed documents**. [ProgrammersGuide.pdf, §8, pp. 269-270, 279; UsersGuide_HIL.pdf, §2.2.4, pp. 12-19; §2.2.6, pp. 20-23]

What must be exposed/configured if a supported Speedgoat route is obtained:

- Put the CarMaker subsystem and the existing virtual-VCU/inverter model in one fixed-step generated model/application. For generated plug-ins, the documented baseline is fixed-step, `ode1`, 0.001 s and SingleTasking; variable-step solvers are not supported. This is a CarMaker generated-plugin requirement, not proof of the existing target model's solver settings. [ProgrammersGuide.pdf, §8.5.1, p. 278]
- Preserve the CarMaker block-chain ordering and use `Sync_In`/`Sync_Out` to select the point at which dictionary quantities are read/overridden. [ProgrammersGuide.pdf, §6.2.2, p. 181]
- Use top-level Inports/Outports in a generated plug-in, which the wrapper moves to/from CarMaker C variables around `DoOneStep`; alternatively use the documented CarMaker `From/To CM Var` blocks. [ProgrammersGuide.pdf, §8.5.3, p. 282]
- Use one of the documented model-class wrappers (for example `VehicleControl`, `PTMotor`, `PTMotorCU`, `PowerTrain`, `Vehicle_Car`) when it matches the intended insertion point; otherwise the Plain wrapper requires manual calls. The interface buses and their signal names are documented in the CarMaker subsystem tables. [ProgrammersGuide.pdf, §8.2.2, pp. 271-273; §8.3, pp. 274-275; §6.3, pp. 228-234; §8.6, p. 284]

The existing `src_cm4sl` project corroborates the local installation context, not Speedgoat compatibility: `CM_Main.c` identifies CarMaker 12.0.1 and includes `CarMaker4SL.h`; `CM_Vehicle.c` registers CM4SL vehicle components; `IO.c` has `-io can`/`-io flexray` configuration entries and CarMaker/HIL MIO initialization. `ModelUpdate_CM12.m` treats `generic.mdl` as a CM4SL model and updates IPG S-functions. None of those files names Speedgoat, its I/O modules, TargetPC1, or an interface protocol for it.

### B. CarMaker/HIL CAN Rest Bus Simulation — documented, but requires CarMaker CAN I/O

The HIL Rest Bus Configurator accepts FIBEX, AUTOSAR, and CANdb network descriptions and generates the communication/interface information plus a mapping table between bus protocol elements and CarMaker quantities/functions. A `.dbc` is an accepted database; ECUs are explicitly classified as simulated or real. [UsersGuide_HIL.pdf, §4, pp. 53, 55-56]

For CAN, the RBS configuration selects one CAN channel at a time. It gets channel information from the IO Configurator and requires that channel to be active. If the CAN interface is not otherwise used, the manual says an `IO.c` `IO_Init()` extension is required to set the slot and initialize the CAN/CANFD module, followed by a freshly compiled CarMaker executable. [UsersGuide_HIL.pdf, §4.5.2 “CAN”, p. 68]

The signal mapping choices include Data Dictionary mapping (with factor/offset/min/max conversion) and constants; the RBS generator also supports rolling counters, CRC/E2E mapping, real-time-expression triggers, and C-code hooks. CAN frame/PDU transmission can be cyclic or on-event; cyclic PDUs use their configured cycle time and CarMaker distributes CAN-frame timing to avoid overflow. [UsersGuide_HIL.pdf, §4.5, pp. 58-62; §4.9, pp. 73-75]

This is a viable mechanism only in this topology:

```text
CarMaker/HIL real-time host + configured physical CAN channel
       ↕ physical, terminated 1-Mbit/s CAN
Speedgoat IO614 Port A / CAN channel 2 ↔ virtual VCU + inverter HIL
```

The manuals support the CarMaker half of that diagram; they do **not** state that a Windows/Office CarMaker virtual CAN network is bridgeable to a Speedgoat CAN port. The documented virtual-CAN module is a CarMaker virtual network (up to eight nodes) enabled with `-io can`; it is not documented as an Ethernet CAN tunnel or external-PC interface. [UsersGuide_HIL.pdf, §3.6.4 “virtual CAN Module - vCAN”, p. 51]

### C. XCP/CCP — documented for ECU calibration/measurement, not verified as a CarMaker vehicle-dynamics link

CarMaker's XCP dialog selects one or more ECUs, reads measurement/characteristic variables from the referenced ASAP2/A2L data, and maps selected variables to CarMaker Data Dictionary quantities. XCP configuration supports CAN (interface selected from the IO Configurator, CAN IDs, CAN-FD settings and DAQ lists) and Ethernet (TCP or UDP preference, IP/hostname, and port); DAQ/STIM events include a cycle time and timestamp options. [UsersGuide_HIL.pdf, §5.1-5.2, pp. 81-85, 98-103, 106-108]

Thus XCP is potentially usable if the external application deliberately implements an XCP ECU/slave interface with a valid A2L and compatible timing. It is **not** documented here as a generic way to step CarMaker's vehicle dynamics remotely, nor do the manuals name a Speedgoat XCP server/client configuration or an XCP data model for this bench. It should not be selected as the primary closed-loop architecture without vendor confirmation and an explicit A2L/role design.

### D. Standalone CarMaker on a separate PC using a generic network protocol — not confirmed

The CM4SL manual documents TCP port 12350 only for Matlab to contact the CarMaker GUI, and the model configuration gives an APO server application name discoverable in IPGControl. It documents dictionary read/write/definition blocks for simulation and monitoring. [ProgrammersGuide.pdf, §6.1.1, p. 174; §6.2.3-6.2.4, pp. 182-185]

Those statements do not specify a real-time user-signal protocol, message schema, peer role, timing contract, or network port for a separate Speedgoat target. I found no local-PDF support for claiming a standalone-PC-over-Ethernet hard-real-time coupling path. The CarMaker FMU also does not fill this gap: it is explicitly limited to CarMaker Office and is driven by an FMI master using `fmi2DoStep`; it is a software co-simulation interface, not a documented Speedgoat deployment path. [ProgrammersGuide.pdf, §10, pp. 319-322]

## 2. Required exposed signals and configuration for this bench

### Minimum closed-loop boundary (recommended design, not an existing CarMaker configuration)

The bench needs an explicit contract; CarMaker does not infer the virtual-VCU CAN contract from vehicle dynamics. A conservative boundary is:

| Direction | Boundary signal family | Why / local basis |
|---|---|---|
| CarMaker → bench | accelerator demand, service-brake demand, key/driver-state as required | CarMaker exposes Driver/VehicleControl gas, brake, key, selector, gear and steering interfaces. [ProgrammersGuide.pdf, §6.3, pp. 229-230] |
| CarMaker → bench | wheel speeds/vehicle velocity and, if modeled, motor/shaft speed/load | CarMaker documents wheel/powertrain interface quantities, but the exact virtual-VCU consumption and scaling are not established in this repository. [ProgrammersGuide.pdf, §6.3, pp. 231-234] |
| Bench → CarMaker | actual/available drive torque, inverter/VCU fault and readiness state | This is needed for a physical closed loop, but no existing virtual-VCU-to-CarMaker mapping was found. The actual bench currently exchanges inverter frames, not CarMaker signals. |

For embedded CM4SL, these should be named Simulink ports or dictionary variables. `Read CM Dict`, `Write CM Dict`, `Define CM Dict`, and Signal Access blocks are documented ways to read, write, define, and make quantities visible to IPGControl/DVA; they are not a substitute for the Speedgoat I/O or CAN marshalling block itself. [ProgrammersGuide.pdf, §6.2.3-6.2.4, pp. 182-185]

For CAN RBS, import `MFE26_Inverter.dbc`, select real versus simulated ECUs, activate/select the physical CAN channel in CarMaker's IO Configurator, map each selected signal to a CarMaker Data Dictionary quantity/function, and configure the DBC-defined frame cycle/trigger behavior. The configurator saves the RBS mapping and CAN communication information in `Data/Config`; it can also place the resulting RBS parameters reference in `ECUParameters`. [UsersGuide_HIL.pdf, §4.2, pp. 55-56; §4.5, pp. 58-62; §4.6, pp. 70-71; §4.9, pp. 73-75]

For XCP, define the ECU side/A2L, activate the ECU in the XCP configuration, select only required A2L variables and map them to Data Dictionary entries, then configure the selected transport's identifiers or IP/port plus DAQ/STIM timing. This is a separate ECU measurement/stimulation design; it does not consume the existing CAN DBC automatically. [UsersGuide_HIL.pdf, §5.1-5.2, pp. 81-85, 98-107]

## 3. Assessment against the actual TargetPC1 CAN contract

### Verified bench facts

- TargetPC1 is `10.10.10.5`. `PINOUTS.md` identifies IO614 as the four-channel HS-CAN/LIN module, maps connector A to CAN channel 2 and connector B to CAN channel 1, and records the virtual VCU on Port A/channel 2 at 1 Mbit/s. `README.md` says the existing virtual VCU reads inverter status and transmits pedal plus four control frames on that port.
- `virtual-vcu/+virtualvcu/config.m` sets CAN channel 2, Port A, `1e6` bit/s, DLC 8, control IDs `0x186/0x196/0x1A6/0x1B6`, status IDs `0x383/0x385/0x393/0x395/0x3A3/0x3A5/0x3B3/0x3B5/0x400`, and pedal CAN ID `0x1F5` (501 decimal). `README.md` records standard 11-bit IDs and that `0x1F5` is not in `MFE26_Inverter.dbc`.
- The same README records a 1 ms virtual-VCU task with 7.5 s precharge and 1.5 s buzzer states. It expressly says physical CAN ACK and target deployment remain unverified. `PINOUTS.md` records that the IO614 interface itself has no termination and uses HS CANL pin 2 / CANH pin 7, so the physical bus must have two end terminators.

### Is CAN coupling plausible?

**Yes, conditionally:** CarMaker 12.0.1 HIL documents a DBC-driven CAN RBS that can communicate with real ECUs and map received/transmitted DBC signals to CarMaker quantities. The bench's `MFE26_Inverter.dbc` is therefore a suitable input artifact for CarMaker's RBS configuration. [UsersGuide_HIL.pdf, §4, pp. 53-56, 58; §4.5.2 “CAN”, p. 68]

**But it is not proven deployable with the installed components:** the local documentation does not say that CarMaker 12.0.1 can run its CAN RBS on the Speedgoat machine, directly use Speedgoat IO614, or bridge CarMaker virtual CAN across Ethernet into IO614. A real CAN connection requires CarMaker to run on a supported HIL/real-time platform with a configured CAN channel, then cable that channel to the Speedgoat Port A bus with bitrate, 11-bit-ID filters, grounding, and termination checked. The existing Port A already has a virtual VCU transmitter, so the CarMaker ECU assignment must prevent duplicate transmitters for `0x186/0x196/0x1A6/0x1B6`, `0x1F5`, or the status IDs.

### What CarMaker should calculate versus what the bench must marshal

If CAN RBS is chosen, CarMaker should calculate vehicle dynamics locally and expose/map the specific quantities that correspond to DBC signals; its RBS layer then performs the DBC frame encoding/decoding. The existing bench application remains responsible for its current virtual-VCU and inverter state machines. This follows the documented RBS quantity mapping model; it does not mean CarMaker natively understands the MFE26 torque semantics. [UsersGuide_HIL.pdf, §4, pp. 53, 58-62]

If embedded CM4SL is chosen, CarMaker can provide ordinary Simulink signals/dictionary quantities such as gas/brake and wheel/powertrain data. The existing virtual-VCU application would need explicit new signal-to-CAN marshalling to its current `0x1F5` pedal and inverter-control/status contract; neither the bench files nor CarMaker manuals show such a marshaller today. [ProgrammersGuide.pdf, §6.3, pp. 228-234; §8.5.3, p. 282]

## 4. Evidence boundary and recommended decision

### Recommended sequencing

1. Obtain written IPG confirmation of CarMaker 12.0.1 support for the exact Speedgoat Baseline/target OS and R2024b code-generation path. Do not start from an assumption that dSPACE/NI instructions apply to Speedgoat.
2. If unsupported, use a separate supported CarMaker/HIL machine with a physical CAN interface and make `MFE26_Inverter.dbc` the only inter-machine contract. Configure CarMaker RBS as the simulated vehicle-side ECU(s), the Speedgoat virtual VCU/inverter endpoints as real ECU(s), and create an ID ownership/cycle-time table before connecting the bus.
3. If supported in one target application, make the connection at fixed-step Simulink ports/dictionary variables first, then add a deliberately specified CAN marshaller only where the existing VCU/inverter behavior must see frames. Keep one timing owner and avoid algebraic loops.
4. Treat XCP as a secondary calibration/measurement option only after an A2L and master/slave role are defined; do not use IPGControl/APO or GUI TCP as the plant-data transport without documentation from IPG.

## Open questions / unverified

- No local manual passage found that lists **Speedgoat**, `TargetPC1`, Speedgoat IO614, Simulink Real-Time/Speedgoat target files, or a supported CarMaker 12.0.1 deployment recipe for this hardware. Whether CarMaker can be compiled into the existing R2024b Speedgoat application is unverified.
- No local manual passage found for a generic standalone CarMaker ↔ Speedgoat Ethernet real-time signal protocol, its port numbers, synchronization/clock discipline, or latency/jitter limits. The TCP 12350 reference is GUI/Matlab connection only. [ProgrammersGuide.pdf, §6.1.1, p. 174]
- No local manual passage found that turns CarMaker virtual CAN into external CAN, an Ethernet tunnel, or a Speedgoat IO614 endpoint. Virtual CAN therefore cannot be assumed to close this physical bench loop. [UsersGuide_HIL.pdf, §3.6.4, p. 51]
- The exact vehicle model, wheel/motor topology, torque-to-wheel mapping, pedal interpretation, and feedback signals necessary for a meaningful vehicle-dynamics loop are absent from the specified bench contract. `MFE26_Inverter.dbc` defines the inverter-side frames but does not by itself define a CarMaker vehicle interface.
- Physical status is not confirmed: the virtual-VCU README explicitly withholds claims of target deployment, Port A/Port B CAN ACK, and analog loopback; CAN termination and transmitter ownership must be checked on the actual wiring before energizing the bus.
- XCP role compatibility is unverified: the manuals document CarMaker's A2L-based ECU configuration and CAN/Ethernet transports, but not a Speedgoat XCP counterpart or an A2L for this HIL application. [UsersGuide_HIL.pdf, §5.1-5.2, pp. 81-85, 101-107]

## 5. Addendum: XCP-over-Ethernet architecture (chosen direction, 2026-08-12)

The chosen connection method is XCP over Ethernet. This addendum resolves the prior document's open question of whether the Speedgoat bench can be the XCP counterpart: it can, when the Simulink Real-Time target application is built and configured in XCP server mode. The local CarMaker manual evidence remains distinct from the newly added web evidence below.

### Evidence boundary: locally verified versus externally sourced

**Locally verified (`UsersGuide_HIL.pdf`).** The existing local research establishes that CarMaker's XCP dialog selects ECU A2L measurement/characteristic variables and maps selected variables to CarMaker Data Dictionary quantities. It also establishes Ethernet transport with TCP or UDP preference, IP/hostname and port configuration, and DAQ/STIM timing options. [UsersGuide_HIL.pdf, §5.1-5.2, pp. 81-85, 98-103, 106-108]

**Externally sourced, not locally re-verified (web).** A controller running on a Speedgoat real-time target can act as an XCP slave/server for parameter tuning and virtual calibration over TCP/UDP with third-party XCP masters; CarMaker would take that same master/client role here. [Speedgoat, XCP over Ethernet](https://www.speedgoat.com/products-services/i-o-connectivity/protocols/xcp-over-ethernet); [MathWorks Answers](https://www.mathworks.com/matlabcentral/answers/430051-speedgoat-xcp-on-ethernet)

Simulink Real-Time supports XCP **server** mode, which emulates an ECU, over real-time UDP as an Ethernet transport. This is distinct from XCP client mode, which bypasses an external real ECU and serves a different use case; server mode is applicable here. [Simulink Real-Time XCP](https://www.mathworks.com/help/slrealtime/xcp.html); [XCP UDP Configuration](https://www.mathworks.com/help/slrealtime/io_ref/xcpudpconfiguration.html)

**Correction to an earlier draft of this addendum:** the generic Embedded Coder `ert.tlc`/`coder.asap2.export` workflow cited below is the desktop/host external-mode XCP path, not the Simulink-Real-Time-specific one. MathWorks documents a distinct Simulink Real-Time workflow instead: configure model data elements for ASAP2 generation, mark the specific signals (measurement/logging) and parameters (tunable/calibratable) that should be exposed, build the Simulink Real-Time target application, then run the separate "Generate Calibration Files" step to produce `model.a2l`. MathWorks explicitly warns that an A2L produced by plain Simulink Coder is missing XCP-specific sections that Simulink Real-Time's own generation step adds — so the generic Embedded Coder A2L workflow is not a substitute. This still requires an Embedded Coder license (Simulink Real-Time's code generation is Embedded-Coder-based), but the actual A2L-producing step is SLRT's "Generate Calibration Files" tool, not a bare `coder.asap2.export` call. [MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)

### Confirmed end-to-end role and data-flow architecture

```text
CarMaker 12.0.1 XCP master/client
  selected A2L variables <----------------------> DAQ/STIM measurement/calibration
  real-time UDP/Ethernet
Speedgoat Simulink Real-Time target application (the existing virtual-VCU + inverter_hil build)
  Signals marked for logging + parameters marked tunable -> "Generate Calibration Files" -> model.a2l
  XCP server/slave (virtual-VCU model presented as the ECU)
```

CarMaker is the XCP master/client, as locally documented; the Speedgoat/Simulink Real-Time application is the XCP server/slave, as externally sourced above. The A2L is generated from the SLRT target build via the "Generate Calibration Files" tool, so it describes only the signals/parameters deliberately marked for that purpose in the model. The real-time UDP transport and server role are externally sourced, not locally re-verified facts. [UsersGuide_HIL.pdf, §5.1-5.2, pp. 81-85, 98-103, 106-108; MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)

### Two independent minimal-exposure control points

At the Speedgoat/model-build stage, only explicitly tunable parameters and signals marked for logging/measurement are included in the generated A2L; no model signal is exposed by default. This is the bench-side mechanism for exposing only necessary parameters. [MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)

Independently, at the CarMaker XCP dialog, the user selects only the required A2L variables to map into the CarMaker Data Dictionary. [UsersGuide_HIL.pdf, §5.1-5.2, pp. 81-85, 98-103, 106-108] Together these controls satisfy the stated goal of exposing only necessary parameters: first constrain the Speedgoat A2L content, then constrain the subset CarMaker imports/maps.

### Pedal-input path and HIL coverage — resolved by the team's stated plan

The earlier draft of this addendum flagged an open question: if CarMaker pedal demand arrived via XCP directly into the Simulink signal path, it would bypass the physical IO183 AO-to-AI loopback and make that stage software-in-the-loop instead of hardware-in-the-loop.

The team has since clarified the actual plan: **the physical voltage loopback (IO183 AO01-AO04 to AI01-AI04) and the physical CAN bus (IO614) both stay in place.** `step.m` continues to receive `measuredVoltageV` from real AI hardware fed by a real analog loopback, exactly as `PINOUTS.md` and `virtual-vcu/README.md` describe today, and inverter control/status continues over the existing physical CAN contract on Port A/channel 2. [Local source: `PINOUTS.md`; `virtual-vcu/README.md`; `+virtualvcu/config.m`; `+virtualvcu/step.m`]

This resolves the coverage question: pedal-read and CAN stay hardware-in-the-loop. It also clarifies what "the CAN loop" actually is: the IO614 Port A / Port B loopback is **internal to the Speedgoat target** — the virtual VCU transmits control frames that `inverter_hil` receives, and `inverter_hil`'s status frames feed back to the virtual VCU. This loop pre-dates and is independent of any CarMaker integration; it is not a bus CarMaker sits on. [Local source: `PINOUTS.md` §5; `virtual-vcu/README.md`]

**CarMaker's only connection to this bench is the XCP-over-Ethernet link.** There is no separate physical CAN path between CarMaker and the Speedgoat, and none is needed — CarMaker does not have a reason to independently decode `MFE26_Inverter.dbc` off a bus it was never wired onto. Consequently, XCP is not merely a diagnostic side-channel: it is CarMaker's *only* source for any control-relevant data it needs (e.g. torque request/estimate, whatever feeds its vehicle-dynamics physics), alongside whatever supplementary state/fault visibility it also wants. There is no fallback data path to lean on.

**Resolved:** CarMaker does not drive the AO channels itself, and pedal demand is not injected directly into the Simulink signal path either. Instead, CarMaker sends throttle/brake demand as an XCP value, and the Speedgoat-side model generates the actual analog voltage from it — i.e. the XCP-received value becomes the new source for the *existing* pedal-voltage-generation stage.

`PINOUTS.md` §4.2 already documents this stage: `pedalVoltageCalibration` maps a `throttle` command (sourced today from dictionary entry `hil_cmd_pedals_throttle`) to AO01/AO02, and a `brake` command (`hil_cmd_pedals_brake`) to AO03/AO04, using the per-channel released/pressed calibration endpoints. Today those dictionary values are written by the bench's own GUI sliders. The proposed change is only to the *source* of those two values — write them from the XCP-received CarMaker demand instead of the GUI — while everything downstream is unchanged: AO01-04 still generate a real voltage, the physical loopback to AI01-04 still carries it, and `virtualvcu.step()` still reads it as real hardware exactly as it does today. This keeps the pedal-read stage genuinely hardware-in-the-loop; CarMaker's contribution is a demand percentage over XCP, not a voltage or a bypass of the AI read.

### Proposed minimal XCP boundary (not an existing implementation)

Since XCP is CarMaker's only link to the bench, the boundary needs to include genuine control-relevant data, not just diagnostics. This is grounded in the named virtual-VCU contract; it is a proposal, not evidence that these variables are currently selectable in an A2L or wired to XCP.

| Direction | Candidate current signal/variable | Proposed use and limitation |
|---|---|---|
| CarMaker -> Speedgoat | Throttle demand, brake demand (two scalars, 0-100%) | Replaces the GUI as the *source* written into dictionary entries `hil_cmd_pedals_throttle`/`hil_cmd_pedals_brake` (`PINOUTS.md` §4.2), which already drive AO01-04 via `pedalVoltageCalibration`. Downstream (AO drive, physical loopback, AI read) is unchanged — this only redirects where the two command values come from. |
| Speedgoat -> CarMaker | Decoded torque-request value (the Nm quantity `step.m` computes as `15 * mean([t1 t2]) / 100 * double(drive)` before packing into `controlPayloads`) | The actual control-relevant number CarMaker's vehicle-dynamics model would need; today it exists only as an intermediate value packed into raw CAN bytes, not as a named, independently exposed signal. Exposing it via XCP would require a deliberate model change to surface it as its own signal/tunable, not a reuse of an existing named output. |
| Speedgoat -> CarMaker | `state`, `stateId`, `enabled` | Publish the state-machine observable state (`LV_ON`, `PRECHARGING`, `ENABLE`, `BUZZING`, `RTD`, `ERROR_SHUTDOWN`) and enable status — needed for CarMaker to know whether torque authority exists at all (e.g. zero outside RTD). |
| Speedgoat -> CarMaker | `valid`, `appsPlausible`, `brakePlausible` | Pedal-plausibility/fault-relevant status computed inside the virtual VCU; with no CAN path to CarMaker, XCP is the only way it could see this. |
| Speedgoat -> CarMaker | Selected `can.dcLink12V`/`can.dcLink34V`, `can.inverterReady`, `can.inverterFault` | Decoded inverter/DC-link status already available inside the virtual VCU's context; CarMaker has no other way to see it. |
| CarMaker -> Speedgoat | Scenario/test-control signals only (e.g. a run/reset or fault-injection trigger), if needed | Not part of the existing `step.m` inputs; a deliberate new addition. |

`measuredVoltageV`, `digitalInputs`, `pedalPct`, `controlPayloads`, and `pedalPayload` stay off this list only insofar as pedal stimulus and CAN framing remain physical per the team's plan; if CarMaker turns out to need to *drive* pedal voltages itself rather than just observe torque/state, that changes this table.

### What to do next

1. The prior largest XCP question is now resolved to **yes** via Simulink Real-Time XCP server mode: mark a small set of signals/parameters in the existing virtual-VCU model, build, run "Generate Calibration Files," and prove minimal CarMaker-to-target DAQ/STIM exchange before expanding the boundary. See §7 for the concrete Speedgoat-side checklist. [MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)
2. Re-check the exact locally installed `UsersGuide_HIL.pdf` pages already cited above to confirm the actual CarMaker 12.0.1 XCP-over-Ethernet transport choices and UDP-versus-TCP configuration specifics. This remains a local-install confirmation task.
3. Confirm that the Speedgoat/Simulink Real-Time environment has an Embedded Coder license available; Simulink Real-Time's code generation and calibration-file workflow depend on it. [MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)

## 6. CarMaker-side build checklist

This checklist implements the settled boundary only: two 0--100% pedal-demand values from CarMaker to the Speedgoat, and the specified torque/state/fault/inverter values in the other direction. It does not add a CarMaker CAN connection or replace the bench's analog or CAN loops. The CarMaker-side MATLAB/Simulink work, if used, must use the team's R2022a installation; the Speedgoat model/build remains the separate R2024b concern.

### Prerequisites before XCP can be configured

1. **Required -- obtain the generated Speedgoat A2L before opening the new-ECU setup.** Supply the A2L that belongs to the exact `ert.tlc` target build, containing the two writable demand variables and the selected readback variables. CarMaker's **New** XCP configuration is created by selecting an A2L database (optionally with an import filter); its variable list is populated from the ASAP2 parameters referenced by the loaded XCP parameters, and the names are those in the ASAP2 file. [UsersGuide_HIL.pdf, p. 89, §5.2 “New”; p. 82, §5.1 “List of ECU variables”]

2. **Required -- agree the A2L-facing signal contract with the Speedgoat build owner.** Include exactly the two demand values (throttle and brake, 0--100%) as variables usable for stimulation, and the decoded torque request, VCU state/enable, plausibility/fault flags, and selected inverter/DC-link status as measurement variables. Also obtain the target IP/host name, XCP Ethernet port, supported TCP/UDP choice, and the DAQ/STIM event names, directions, cycle times, and limits that the A2L/server exposes. CarMaker's setup exposes Ethernet protocol, IP, host name, and port, while event direction can only be selected when the A2L event permits DAQ or STIM; the manual does not provide these values for this Speedgoat application. [UsersGuide_HIL.pdf, p. 102, §5.2 “Transport Layer -- Ethernet”; p. 86, §5.1 “Sample Groups”; pp. 98-101, §5.2 “DAQ”]

3. **Required -- work in a CarMaker project and have a TestRun available to start the application.** CarMaker work is organized in a project directory, with GUI-authored TestRun data in `Data`; a batch startup file likewise makes `TestRun` mandatory and resolves it under `<ProjectDir>/Data/TestRun`. [ProgrammersGuide.pdf, p. 20, §1.1.3; ReferenceManual.pdf, pp. 54-55, §3.7]

3a. **Resolved -- this is not a from-scratch build; use the team's existing `FS_race` project and its `MFE26_V1` vehicle.** A full-drive scan of this machine found the team's actual maintained CarMaker project at `C:\Users\MFE-HPC\Documents\GitHub\IPG-MFE` (GitHub remote `McGillFormulaElectric/IPG-MFE`), containing `FCM_Projects\FS_race` and `FCM_Projects\FS_autonomous`. The user confirmed `FS_race` is the correct one for this work. `FS_race\Data\Vehicle\Examples_FS` already contains an MFE26-generation vehicle object, `MFE26_V1` (alongside the prior `MFE24_V1`-`V4` lineage), so the vehicle/aero/powertrain characterization does not need to be recreated. The companion launch script `IPG_MFE_Launch.m` explicitly requires **MATLAB R2022a** (matching the CarMaker 12.0.1 Simulink-integration ceiling noted in the executive summary above) and references a separate `MFE25-Controls` repo and a `MFE24ControlsMFE26` repo, indicating the team's controls work is mid-migration from the MFE24/25 generation onto MFE26 — worth coordinating with whoever owns that migration before adding the XCP interface, since both efforts touch the same project. At the time of this research, the local `IPG-MFE` checkout was on branch `parameters` (`cc79e44`, tracking `origin/parameters`) with uncommitted local modifications under both `FS_race` and `FS_autonomous` `Data/Config` (e.g. `ECUParameters`, `DVA`, `Preferences.sce`, `.IPGControl.conf`) — these look like ordinary CarMaker-GUI-generated local state rather than deliberate engineering changes, but confirm with the project owner before building on top, since `ECUParameters` is exactly the file the XCP configuration in this checklist will also modify (item 8 below), and local uncommitted state there could conflict with or be overwritten by that work. A separately downloaded `FS_LTS_2026_v1` package (with its own `Aero_4EM`/`Nero_4EM` vehicle variants) is IPG's own generic Formula-Student Lap-Time-Simulation product deliverable, per its bundled "LTS Documentation" PDF, not a McGillFormulaElectric project; treat it only as a possible cross-reference, not the base to build on. [Local source: `C:\Users\MFE-HPC\Documents\GitHub\IPG-MFE` directory scan, `git log`/`git remote -v`/`git status`/`git branch -vv` output, `IPG_MFE_Launch.m`, `FCM_Projects\FS_race\Data\Vehicle\Examples_FS` directory listing, `Downloads\FS_LTS_2026_v1\FS_LTS_2026_v1\LTS Documentation  Formula Student 2026 v1.pdf` filename]

4. **Only required if the checklist is being used to produce vehicle-dynamics results rather than merely prove XCP exchange -- prepare a valid vehicle/road/TestRun combination appropriate to the desired scenario.** The installed manuals establish that the vehicle model is initialized and that a Road file can be specified, but they do not document a supported “XCP-only, no vehicle/road” runtime mode or identify the absolute minimum valid vehicle-data and road files. A full maneuver is not evidenced as mandatory: `DrivMan.nMan` defaults to zero. Treat the exact smallest runnable scenario as an IPG/GUI confirmation item rather than assuming that a complete maneuver or a particular road is required. [ReferenceManual.pdf, p. 45, §3.1.13; p. 69, §4.5.1; p. 79, §6.3]

### Create and save the XCP ECU configuration

5. **Required -- open the XCP setup from the CarMaker GUI and create the ECU entry.** When XCP is available, use the XCP LED at the lower right of the main GUI or the corresponding **Realtime System** menu entry. If `ECUParameters` has no `XCPParameters` entry, CarMaker automatically raises XCP Setup; in XCP Setup choose **New**, select the supplied A2L, optionally scan for naming conflicts, and set a distinct ECU name for the Speedgoat application. [UsersGuide_HIL.pdf, pp. 79-80, Chapter 5 introduction; p. 89, §5.2 “New”; p. 92, §5.2 “General”]

6. **Required -- configure the Ethernet transport to the Speedgoat XCP server.** In XCP Setup's General/Transport Layer configuration, choose Ethernet and enter the agreed protocol preference, IP address or host name, and port. Do not invent CAN identifiers or configure CarMaker CAN I/O: CAN is a separate transport-layer pane, whereas the settled design uses Ethernet only. [UsersGuide_HIL.pdf, p. 92, §5.2 “General”; p. 102, §5.2 “Transport Layer -- Ethernet”]

7. **Required -- accept or reconcile A2L-derived protocol/DAQ settings, then configure only the allowed event timing.** Review the A2L-derived protocol, DAQ capability, timestamps, events, and transport settings in XCP Setup. For every event used by this interface, use the A2L-provided event channel and permitted direction; configure its sample-group rate as a multiple of that event's base rate. If the selected A2L changes, use the XCP Diff dialog to decide which database parameters update the setup; defined A2L DAQ events not already in Setup are added automatically. [UsersGuide_HIL.pdf, pp. 96-101, §5.2 “Protocol” and “DAQ”; p. 108, §5.2 “XCP Diff dialog”; p. 86, §5.1 “Sample Groups”]

8. **Required -- save under `Data/Config` and activate the configuration in `ECUParameters`.** Use **Save**/**Save as**, give the XCPParameters InfoFile a project-local name, and select **Activate in ECUParameters**. XCPParameters can only be saved/loaded from a `Data/Config` subdirectory; saving can insert the configuration name into `ECUParameters`, and one XCPParameters file can contain configurations for multiple ECU interfaces. [UsersGuide_HIL.pdf, pp. 83-84, §5.1 “Configuration Buttons”; p. 91, §5.2 “Save Dialog”]

9. **Required -- activate the Speedgoat ECU in XCP Configuration.** Select the ECU in the ECU selection menu and set its state to active; only active ECUs participate, and the documented states are non-active, active, and connected. [UsersGuide_HIL.pdf, p. 81, §5.1 “ECU Selection”]

### Create quantities and perform the actual mappings

10. **Required -- add the new Speedgoat-to-CarMaker measurement quantities through the UAQs selector.** In XCP Setup's UAQs window, move only the required A2L measurement quantities (decoded torque request, VCU state/enable, plausibility/fault flags, and selected inverter/DC-link status) from **Available** to **Selected**. The manual states that selected UAQs are added to the Data Dictionary and explicitly recommends limiting them because an A2L can define many measurements. This is the documented no-C-code route for creating the new CarMaker-side readback entries. [UsersGuide_HIL.pdf, pp. 106-107, §5.2 “UAQs”]

11. **Only required if an A2L/UAQ name or representation is unsuitable for the CarMaker model -- define a separate CarMaker UAQ in code/model and then map to it.** The documented native C-code route is a `DDefXXX()` function from `DataDict.h`, called from `User_DeclQuants()` or a model's `xxx_DeclQuants()`, with a stable C variable, name, unit, and DVA access point. Register each quantity once only; use `DDefStates()` for discrete state metadata where appropriate. This is an alternative to, not evidence that the XCP UI automatically creates a custom renamed/typed quantity. [ProgrammersGuide.pdf, pp. 101-102, §4.2; pp. 124-126, §5.3.1]

12. **Required -- assign each selected XCP variable to a sample group, then map it manually.** In XCP Configuration, select the variable, assign it to a DAQ list/sample group, and then use the Mapping tab's **DDict** mapping: type the target dictionary name or select it with **Q browser**. This is a separate manual operation; the mapping pane is unavailable until the variable is part of a sample group, and the UAQs selection page does not state that it performs those per-variable mappings. Use Factor, Offset, Min, and Max only where a unit/scaling conversion is actually needed. [UsersGuide_HIL.pdf, pp. 82, 85-86, §5.1 “List of ECU variables,” “Mapping,” and “Sample Groups”; pp. 106-107, §5.2 “UAQs”]

13. **Required -- map the two Speedgoat stimulation variables to the existing pedal-command entries.** Map the A2L throttle-demand variable to `hil_cmd_pedals_throttle` and the brake-demand variable to `hil_cmd_pedals_brake`; preserve the agreed 0--100% units/scaling. These entries already exist on the bench side, and the documented XCP mapping control permits the target dictionary entry to be typed or picked via Q browser. The exact GUI behavior for a STIM-direction variable's DDict write is not separately illustrated by the manual, so confirm this on the bench before relying on it. [UsersGuide_HIL.pdf, pp. 85-86, §5.1 “Mapping” and “Sample Groups”; Local source: `PINOUTS.md` §4.2]

14. **Required -- connect CarMaker's vehicle model to the new readback quantities deliberately.** Use the mapped torque request/state/fault/inverter entries in the CarMaker-side model at the chosen vehicle-control/powertrain insertion point, with the state/fault contract determining torque authority. The local manuals document Data Dictionary quantities as the basis for online access and results storage, but they do not prescribe a vehicle-model block, torque conversion, or fault-response implementation for this Speedgoat interface; those are integration choices, not automatically provided by XCP import. [ProgrammersGuide.pdf, p. 101, §4.1; pp. 103-105, §5.1]

### Run and prove the smallest interface first

15. **Required -- enable XCP acquisition and run the selected TestRun.** Turn the XCP LED to orange (**XCP On**) to start acquisition automatically with each TestRun, then start the TestRun. The LED turns dark green during acquisition; connection/disconnection and manual DAQ start/stop are available from the XCP configuration context menu for diagnosis. [UsersGuide_HIL.pdf, pp. 87-89, §5.1 “Communication Control” and “Context Menu”]

16. **Only required if you need records or live diagnosis -- configure storage and inspect the mapped quantities.** XCP Setup can save DAQ data to MDF (including automatic, manual, or data-store-controlled saving); CarMaker's Data Dictionary is also the source for regular online quantity transmission to IPGControl. Add only the quantities needed for the proof to avoid unnecessary real-time overhead. [UsersGuide_HIL.pdf, pp. 92-94, §5.2 “General”; ProgrammersGuide.pdf, pp. 21, 101-102, §§1.1.4, 4.1-4.2]

### Items not confirmed by the installed PDFs

- The manuals do not state the exact Speedgoat XCP server settings, its A2L variable names/types/units, UDP-versus-TCP support in this particular target build, security/Seed&Key requirements, or the available DAQ/STIM event table. Obtain these from the generated A2L and the Speedgoat build/configuration, then verify with a live connection. [UsersGuide_HIL.pdf, pp. 88, 96-102, §§5.1-5.2]
- The manuals do not explicitly demonstrate whether the UAQs page creates dictionary entries with precisely the A2L names, nor whether selecting UAQs also creates a variable-to-DDict mapping; the manual describes these in separate UI areas. Confirm the resulting names and mappings in the GUI/Q browser rather than assuming an import wizard completes them. [UsersGuide_HIL.pdf, pp. 85-86, 106-107, §§5.1-5.2]
- No local PDF establishes a supported minimum “XCP-only” CarMaker runtime with no vehicle data set, road, or physics setup. It confirms a project and TestRun are used to run the application, and that zero maneuvers is the default, but the smallest valid vehicle/road configuration requires IPG support or hands-on GUI validation. [ProgrammersGuide.pdf, p. 20, §1.1.3; ReferenceManual.pdf, pp. 54-55, §3.7; p. 79, §6.3]
- No local PDF specifies how this bench's decoded torque request should drive a CarMaker powertrain/wheel model, or how VCU/inverter fault states should alter that model. Those are deliberate model-design decisions to validate with the vehicle-dynamics owner, not facts supplied by the XCP configuration documentation. [ProgrammersGuide.pdf, pp. 103-105, §5.1]

## 7. Speedgoat-side build checklist

This checklist implements the settled XCP boundary in the already-existing `inverter_hil` Speedgoat application: CarMaker supplies two 0--100% demands, while the target supplies the selected VCU and inverter observations. It does not replace the IO183 analog loopback or the IO614 CAN loop. The local evidence confirms an R2024b, fixed-step, 1 ms `speedgoat.tlc` build that is deployed through `slrealtime` to `TargetPC1`; the XCP-server/A2L workflow below is externally sourced from MathWorks and is not locally re-verifiable. [Local source: `inverter_hil/build_inverter_hil_model.m:25-35`; `inverter_hil/deploy_inverter_hil.m:21-45,67-74`; externally sourced: [MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)]

### Model changes needed

1. **Required -- retain the current deployed model and its real hardware paths.** Work from `inverter_hil`, whose build adds the virtual VCU into the same deployable application, and retain its fixed-step discrete solver, 0.001 s step, `speedgoat.tlc` target, tunable default parameters, and signal logging setting. Do not replace the analog/CAN hardware boundary merely to add XCP. [Local source: `inverter_hil/build_inverter_hil_model.m:25-35,62-69`; `virtual-vcu/models/patch_virtual_vcu_inputs.m:18-30,55,133-138`]

2. **Required -- add two distinct XCP-tunable pedal-demand parameters and make them the selected source for the existing pedal command path.** Add a throttle and brake parameter that represent CarMaker's 0--100% demand, normalize them to the existing 0--1 target range, and route the selected source into the two existing dictionary-backed command parameters `hil_cmd_pedals_throttle` and `hil_cmd_pedals_brake` (rather than bypassing the AO/AI loop). Those dictionary parameters are presently created with 0--1 limits and are the inputs verified for the pedal-emulation mapping. [Local source: `inverter_hil/build_inverter_hil_model.m:94-99`; `verify_pinouts.m:142-148`; `PINOUTS.md:224-225`]

3. **Required -- implement the resolved XCP-over-GUI ownership policy before wiring the new parameters.** When CarMaker/IPG is connected through XCP and actively driving pedal demand, XCP writes take precedence and the GUI is diagnostic/read-only: it must not write either pedal dictionary entry. Concretely, gate the two GUI coalescer write calls, `app.commitWrite('pedals.throttle', ...)` and `app.commitWrite('pedals.brake', ...)`, on a live “XCP connected and driving pedal demand” indication so that they do not reach `Session.write`; that method otherwise resolves the pedal logical names to `hil_cmd_pedals_throttle`/`hil_cmd_pedals_brake` and calls `Backend.setparam`. Keep the GUI's display/readback path independent of write ownership: it must continue to obtain and show the current dictionary value, including one written by XCP, rather than treating the slider as the source of truth. The existing session already has a read-only target-value path using `Backend.getparam`, but the requested local files contain no XCP-session/active-demand indication to use for the new gate. **On XCP disconnect, GUI write control reverts automatically:** the moment the "XCP connected and driving" indication used for the gate goes false, the GUI coalescer write calls must resume reaching `Session.write` without requiring a separate re-arm action from the operator. This is now a stated decision, not an open assumption; it still depends on the same not-yet-implemented connection-detection mechanism as the forward direction, and should be built as one gate (GUI writes enabled if and only if XCP is not actively driving), not two independently-triggered behaviors. [Local source: `inverter_hil/inverter_hil_app.m:898-912`; `inverter_hil/+inverterhilgui/parameterContract.m:24-29`; `inverter_hil/+inverterhilgui/targetSession.m:157-200,716-730`]

4. **Required -- surface torque request as a named numeric signal before CAN packing.** In the reference algorithm, `virtualvcu.step` constructs `out` and then computes `15 * mean([t1 t2]) / 100 * double(drive)` directly as the fourth argument of `packControlFrame`, so no named torque value exists to mark for measurement. Introduce one named decoded torque-request signal (with its unit and scaling stated) before that packing operation, and preserve the existing control-payload behavior. [Local source: `virtual-vcu/+virtualvcu/step.m:66-80`]

5. **Required -- make the equivalent deployed-chart signal observable as well.** The actual model generator loads `virtualVcuDeployStep.m` into the `Virtual VCU LV_ON` MATLAB Function block; that chart currently calculates `torque`, gates it with `drive`, and puts only its raw bytes in `payloads(13:14)`. Extend that chart/output contract and the model plumbing so the named decoded torque-request signal, rather than a reconstructed CAN payload, reaches an observer suitable for measurement. [Local source: `virtual-vcu/models/add_virtual_vcu_to_model.m:191-207,251-253`; `virtual-vcu/models/virtualVcuDeployStep.m:177-193`]

6. **Required -- expose each agreed Speedgoat-to-CarMaker value as a typed, named observer signal.** Add observers for the decoded torque request; `state`/`stateId`/`enabled`; `valid`, `appsPlausible`, and `brakePlausible`; and selected `can.dcLink12V`, `can.dcLink34V`, `can.inverterReady`, and `can.inverterFault`. The existing source-level contract already has these fields, but the existing deployed chart/observer does not expose the full set, so they must be deliberately carried through the deploy implementation. [Local source: `virtual-vcu/+virtualvcu/step.m:68-75`; `virtual-vcu/models/virtualVcuDeployStep.m:1-4,194-197`; `virtual-vcu/models/patch_virtual_vcu_state_outputs.m:49-58,150-153`]

7. **Required -- reuse the existing observer pattern, not a dead-end signal tap.** Put every new value behind a `Virtual VCU Observability` subsystem output using the existing global-Goto → From → Signal Conversion (`Signal copy`) → Outport pattern, and mark the copied output as a TestPoint. This is the local, proven mechanism that keeps observability-only values from being optimized into the wrong storage and is what the GUI presently reads through `getsignal`. [Local source: `virtual-vcu/models/patch_virtual_vcu_state_outputs.m:98-101,118-149,154-185`; `inverter_hil/+inverterhilgui/targetSession.m:350-375`]

8. **Only required if the current GUI must continue displaying a newly added value -- extend its readback mapping separately.** The GUI reads numbered observer ports with `getsignal`; adding an XCP measurement signal does not automatically add a GUI field or preserve an old port-number contract. Update that GUI mapping only for values the GUI must display; XCP measurement exposure itself should use the marked named signals from the preceding steps. [Local source: `inverter_hil/+inverterhilgui/targetSession.m:350-410`; `virtual-vcu/models/patch_virtual_vcu_state_outputs.m:150-185`]

### Marking signals and parameters for XCP

9. **Required -- explicitly mark only the two new pedal-demand parameters as tunable/calibratable and only the agreed readbacks as logging/measurement signals.** In the Simulink Real-Time workflow, A2L content is restricted to model elements explicitly configured for ASAP2 generation: marked tunable parameters become calibratable entries and marked signals become measurement/logging entries. Include the two commands and exactly the signals in item 6; do not assume that ordinary model signals, GUI observers, or all dictionary entries appear automatically. [Externally sourced, not locally re-verifiable: [MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)]

10. **Only required if a signal is presently represented only as a CAN byte vector or unnamed expression -- complete items 4--7 before marking it.** A TestPoint/Signal Copy preserves an existing named model signal for observation, but it does not create the missing decoded torque-request semantic or the omitted source-level fields. Mark the newly named, typed signals rather than raw `controlPayloads` bytes. [Local source: `virtual-vcu/+virtualvcu/step.m:73-80`; `virtual-vcu/models/patch_virtual_vcu_state_outputs.m:118-149`; externally sourced: [MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)]

### Build and generate the A2L

11. **Required -- rebuild the normal Simulink Real-Time application after the model-data and observer changes.** Run the existing build/deploy flow against the same R2024b workspace and application name; it uses `slbuild(model)`, then connects to `TargetPC1`, stops a running application, loads, starts, and sets the startup application. [Local source: `inverter_hil/inverter_hil_setup.m:1-39`; `inverter_hil/deploy_inverter_hil.m:21-75`]

12. **Required -- generate the calibration files as a separate Simulink Real-Time step and use the resulting `model.a2l`.** After the normal target build, run Simulink Real-Time's **Generate Calibration Files** tool. Do not substitute a generic Embedded Coder `ert.tlc`/`coder.asap2.export` A2L: MathWorks documents that the SLRT-specific generation step supplies XCP-specific content absent from a plain Simulink-Coder A2L. [Externally sourced, not locally re-verifiable: [MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)]

13. **Required -- inspect the generated A2L before handing it to CarMaker.** Confirm that it contains exactly two writable, clearly unit-scaled pedal-demand parameters and the named torque/state/plausibility/inverter measurements selected above. If an item is absent, correct its model-data/measurement marking and regenerate the calibration files; do not add a generic A2L as a workaround. [Externally sourced, not locally re-verifiable: [MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)]

### Configure the XCP UDP target endpoint

14. **Required -- configure the Simulink Real-Time target application in XCP server mode with UDP as the agreed transport.** Use the generated A2L/server configuration as the contract consumed by CarMaker; this is XCP server mode (the target emulates an ECU), not SLRT XCP client mode. Record the selected UDP endpoint with the A2L delivery. [Externally sourced, not locally re-verifiable: [MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)]

15. **Only required if the endpoint differs from the bench default target identity -- update the deployment and handoff record consistently.** The local deployment helper defaults to `TargetPC1`, while the bench documentation identifies that target as `10.10.10.5`; neither local build script sets an XCP UDP port. Keep the target identity distinct from the as-yet-unselected XCP port. [Local source: `inverter_hil/deploy_inverter_hil.m:21-23,45-48`; `PINOUTS.md:1-10`]

### Run and verify

16. **Required -- prove a minimal command path with the physical loop intact.** With the application running, stimulate throttle and brake through XCP and verify that the selected source changes the existing dictionary-backed command path, AO01--AO04, the physical analog loopback, and the virtual-VCU AI input; do not validate by bypassing that hardware path. [Local source: `PINOUTS.md:224-225`; `virtual-vcu/models/patch_virtual_vcu_inputs.m:18-30,55,133-138`; externally sourced: [MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)]

17. **Required -- prove the minimal measurement path at the generated-A2L level.** In the XCP master, read the named decoded torque request and each selected state/plausibility/inverter signal while exercising a known state transition and pedal change; verify zero torque when its existing drive gate is false and compare the measurements with the local observer/GUI where that observer exists. [Local source: `virtual-vcu/+virtualvcu/step.m:66-80`; `virtual-vcu/models/virtualVcuDeployStep.m:177-193`; `inverter_hil/+inverterhilgui/targetSession.m:350-410`; externally sourced: [MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)]

18. **Only required if a rebuild changed model observability or the deployed application -- rerun the local structural verifier before live XCP testing.** It checks the `Virtual VCU` boundary, IO614 channel 2, five CAN writes, and the expected control/pedal IDs, protecting the existing physical-CAN contract while the XCP observers are added. [Local source: `virtual-vcu/models/verify_virtual_vcu_model.m:7-37`]

### Items not confirmed locally

- **Embedded Coder licensing is not confirmed locally.** The repository confirms R2024b and Speedgoat block-library discovery, but it contains no local license inventory proving that the Speedgoat/Simulink Real-Time environment has Embedded Coder available. The external SLRT calibration-file workflow identifies the product workflow but cannot prove this installation's license state. [Local source: `inverter_hil/inverter_hil_setup.m:1-29`; externally sourced, not locally re-verifiable: [MathWorks, Calibrate Real-Time Application](https://www.mathworks.com/help/slrealtime/ug/calibrate-real-time-application.html)]
- **The exact XCP UDP endpoint is not confirmed locally.** `TargetPC1` is recorded as `10.10.10.5`, but no inspected local file configures an XCP server port; choose and record the UDP port with the target/network owner before configuring CarMaker. [Local source: `PINOUTS.md:1-10`; `inverter_hil/deploy_inverter_hil.m:21-23,45-48`]
- **The XCP-precedence policy, including reversion on disconnect, is resolved; its local connection-detection mechanism is not.** The decided behavior is: while CarMaker/IPG is XCP-connected and actively driving pedal demand, XCP owns the two pedal dictionary entries and the GUI is diagnostic/read-only; the moment that connection/active-drive condition goes false, GUI write control resumes automatically, with no separate re-arm step. Both directions are now user-stated decisions, not assumptions. What remains open is purely mechanical: the requested local GUI code has a reusable *GUI-to-target* lifecycle pattern (`targetSession` stores `State`, connects/disconnects its backend, and derives GUI target/application state from that backend), but it does **not** expose an XCP-client connection or active-pedal-demand state, so it cannot by itself drive the single gate item 3 now requires in both directions. The earlier externally sourced research on the Simulink Real-Time XCP UDP Configuration block's connection-status output ports may be the mechanism to adapt, but that has not been verified locally against this particular GUI/model. [Local source: `inverter_hil/inverter_hil_app.m:898-912`; `inverter_hil/+inverterhilgui/parameterContract.m:24-29`; `inverter_hil/+inverterhilgui/targetSession.m:27-34,59-126,157-200,703-730`; externally sourced, not locally re-verifiable: [XCP UDP Configuration](https://www.mathworks.com/help/slrealtime/io_ref/xcpudpconfiguration.html)]
